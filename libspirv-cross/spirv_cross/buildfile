intf_libs = # Interface dependencies.
impl_libs = # Implementation dependencies.
#import xxxx_libs += libhello%lib{hello}

lib{spirv-cross}: {hxx ixx txx cxx}{**} $impl_libs $intf_libs

# Build options.
#
cxx.poptions =+ "-I$out_root" "-I$src_root"

#obja{*}: cxx.poptions += -DSPIRV_CROSS_STATIC_BUILD
#objs{*}: cxx.poptions += -DSPIRV_CROSS_SHARED_BUILD

# Export options.
#
lib{spirv-cross}:
{
  cxx.export.poptions = "-I$out_root" "-I$src_root"
  cxx.export.libs = $intf_libs
}

#liba{spirv-cross}: cxx.export.poptions += -DSPIRV_CROSS_STATIC
#libs{spirv-cross}: cxx.export.poptions += -DSPIRV_CROSS_SHARED

# For pre-releases use the complete version to make sure they cannot
# be used in place of another pre-release or the final version. See
# the version module for details on the version.* variable values.
#
if $version.pre_release
  lib{spirv-cross}: bin.lib.version = "-$version.project_id"
else
  lib{spirv-cross}: bin.lib.version = "-$version.major.$version.minor"

# Install into the spirv_cross/ subdirectory of, say, /usr/include/
# recreating subdirectories.
#
{hxx ixx txx}{*}:
{
  install         = include/spirv_cross/
  install.subdirs = true
}
